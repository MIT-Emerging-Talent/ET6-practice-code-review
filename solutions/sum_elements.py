def sum_elements(numbers):
    """
    Calculate the sum of all elements in a list.

    Args:
        numbers (list): A list of integers or floats.

    Returns:
        int or float: The sum of the list's elements.
                      Returns 0 for an empty list.
    """
    return sum(numbers)

